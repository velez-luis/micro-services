server:
  port: 8080
spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://github.com/velez-luis/micro-services
          search-paths: eureka
  application:
    name: config-service