server:
  port: 8080
spring:
  cloud:
    config:
      server:
        git:
          default-label: eureka
          uri: https://github.com/velez-luis/micro-services
          search-paths: config-data
  application:
    name: config-service